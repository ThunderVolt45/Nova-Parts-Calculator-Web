# AGENTS.md

이 문서는 `Nova-Parts-Calculator-Web`을 개발하거나 수정하는 사람과 자동화
에이전트가 따라야 할 제품 요구사항, 기술 경계, 검증 기준을 정의합니다.

## 현재 단계

- T01~T21의 핵심 구현과 PC·모바일 대응 검증이 완료되었습니다.
- T22 품질 검증 중 접근성, 주요 E2E 흐름, 모델 메모리, 파싱 성능,
  로컬 파일 미전송 검사가 완료되었습니다.
- 현재 기준 검증 결과는 Vitest 156개, Playwright 26개 통과, 린트와
  프로덕션 빌드 성공, 프로덕션 의존성 취약점 0개입니다.
- 프로덕션 산출물에는 GX/XFI/GLB 등 원본 게임 리소스와 소스맵이 포함되지
  않습니다.
- Cloudflare Pages 프로젝트가 생성되어 GitHub `main` 자동 프로덕션 배포가
  활성화되었고, 첫 실제 배포가 완료되었습니다.
- GitHub Actions는 pull request와 `main` push에서 전체 품질 검증을 수행합니다.
- 서비스 전용 파비콘과 비공식 팬 도구 및 개인정보 고지를 준비했습니다.
- 프로젝트, 기준 구현 기여자와 제3자 오픈소스 라이선스 고지를 준비했습니다.
- 기본 보안 헤더와 사용자·배포·운영 문서를 준비했습니다.
- 요구사항과 충돌하는 추측으로 구현 범위를 확장하지 않습니다.
- 구현 작업의 분할, 권장 순서, 진행 상태는 [`TASKS.md`](./TASKS.md)를
  기준으로 관리합니다.
- 모든 커밋은 [`COMMIT_CONVENTION.md`](./COMMIT_CONVENTION.md)의 한 줄
  한글 Conventional Commits 규칙을 따릅니다.

## 프로젝트 개요

이 프로젝트는 노바 1492 부품 조합 능력치를 계산하고, 사용자가 로컬에
설치한 게임 클라이언트의 GX/XFI 리소스를 승인 기반으로 읽어 부품 및 일반
유닛의 3D 모습을 표시하는 반응형 웹 계산기입니다.

기준 프로젝트는 다음 두 저장소입니다.

- `../Nova-Parts-Calculator-Python`
  - 부품 JSON 스키마, 강화 공식, 최종 능력치, 스킬/상태 보정, 조립 유효성
    검사의 기준 구현입니다.
- `../Nova-1492-GX-Unpacker`
  - GX/XFI 파싱, glTF/GLB 변환, 텍스처 처리, 애니메이션 추출, 일반 유닛
    3부품 소켓 조립의 기준 구현입니다.

기준 저장소는 사용자가 별도로 요청하지 않는 한 읽기 전용으로 취급합니다.
전체 작업 로드맵은 [`TASKS.md`](./TASKS.md)에서 확인합니다.

## Git 커밋 규칙

- 커밋 메시지는 `<type>: <한글 작업 요약>` 형식의 제목 한 줄만 사용합니다.
- 본문, 상세 목록, 푸터를 작성하지 않습니다.
- 허용 타입과 예시는 [`COMMIT_CONVENTION.md`](./COMMIT_CONVENTION.md)를
  따릅니다.
- 서로 관련 없는 변경을 한 커밋에 섞지 않습니다.

## 기술 스택

- React
- TypeScript
- Vite
- Three.js
- `@react-three/fiber`
- `@react-three/drei`
- Zustand
- Zod
- Vitest
- Playwright

초기 앱은 정적 배포 가능한 클라이언트 중심 웹 앱으로 설계합니다. 서버
동기화는 초기 버전 범위에서 제외하며 계산기와 로컬 덱은 서버 없이
작동해야 합니다.

브라우저 GX 파서는 기존 Python 파서의 검증된 동작을 TypeScript로 포팅하고
Web Worker에서 실행합니다. Pyodide로 Python 런타임 전체를 포함하지
않습니다.

## 배포 및 공개 운영 원칙

- 초기 호스팅 대상은 Cloudflare Pages이며 GitHub 저장소 연동 방식으로
  배포합니다.
- 프로덕션 브랜치는 `main`, 프레임워크 프리셋은 React(Vite), 빌드 명령은
  `npm run build`, 배포 디렉터리는 `dist`, Node.js 버전은 22를 사용합니다.
- `main` 푸시는 첫 배포 이후 자동 프로덕션 배포를 유발할 수 있으므로
  푸시 전에 최소한 린트, 단위 테스트, E2E 테스트, 프로덕션 빌드를
  통과시킵니다.
- 현재 앱은 단일 경로 정적 앱이므로 SPA 폴백용 `_redirects`를 추가하지
  않습니다. 실제 클라이언트 라우트가 생길 때 다시 검토합니다.
- 정적 응답의 기본 보안 헤더는 `public/_headers`로 관리하고 배포 후 실제
  응답 헤더를 확인합니다. 엄격한 CSP는 Web Worker, Blob URL, 3D 렌더링과
  향후 광고 스크립트 호환성을 검증한 뒤 적용합니다.
- `*.pages.dev` 주소의 검색 노출 여부를 명시적으로 결정합니다. 커스텀
  도메인을 연결하면 중복 검색 노출을 막기 위해 Pages 기본 주소의
  `X-Robots-Tag` 정책을 다시 검토합니다.
- 첫 배포 후 HTTPS 환경에서 로컬 폴더 승인, Firefox 디렉터리 폴백,
  IndexedDB 복원, JSON 가져오기·내보내기, 모바일 3D 비활성화를 실제로
  확인합니다.
- 현재 광고, 분석 SDK, 추적 쿠키는 사용하지 않습니다. 수익화는 보류
  상태이며 사용자가 별도로 승인하기 전까지 광고나 추적 기능을 추가하지
  않습니다.
- 광고나 분석 기능을 도입하기 전 개인정보 안내, 쿠키·동의 처리, 외부 전송
  목록, CSP와 보안 헤더를 함께 갱신합니다.
- 공개 화면에는 비공식 팬 제작 도구임을 밝히고 게임 명칭·상표의 권리자,
  로컬 파일 미전송, 로컬 저장 방식, 문의 및 권리 침해 신고 수단을
  안내합니다.

## 계산기 기능 요구사항

`Nova-Parts-Calculator-Python`의 사용자 기능을 빠짐없이 구현합니다.

- 다리, 몸통, 무기, 액세서리 선택
- 몸통/무기의 탑형, 팔형, 어깨형 구분
- 다리/몸통/무기의 와트, 체력, 공격 강화 수치
- 액세서리 랜덤 체력, 공격, 방어 옵션
- 다리, 몸통, 무기에 장착하는 서브코어 3개
- 정수 계산과 실수 계산 모드
- 무게/하중, 와트, 체력, 리젠, 속도, 연사, 사거리, 최소 사거리, 범위,
  시야, 공격력, 체력 비례 피해, 방어 무시, 방어력, 회복량 등 기존 출력
- 부품과 서브코어의 특수 능력 설명
- 스킬, 상태, 타워링, 데스매치, 진형 등의 기존 최종 능력치 보정
- 초기화 및 조립 결과 갱신

계산 로직은 React 컴포넌트와 분리된 순수 TypeScript 도메인 함수로
작성합니다. Zustand에는 입력 상태를 저장하고 계산 결과는 입력에서
파생합니다.

### 계산 동등성

- Python 구현의 `int()` 절삭 위치, 백분율 적용 순서, 상한/하한, 실수
  반올림을 그대로 보존합니다.
- Python 기준 결과를 골든 테스트 데이터로 생성하고 TypeScript 결과와
  비교합니다.
- 배열 위치보다 안정적인 부품 `ID`를 저장 형식과 앱 상태의 식별자로
  사용합니다.

### 조립 유효성

다음 규칙을 Python 계산기와 동일하게 적용합니다.

- 다리, 몸통, 무기 누락
- 몸통과 무기의 타입 불일치
- 하중 초과
- N 부품 개수 제한
- 아포칼립스의 몸통 무게 및 타워링 제한
- 그 밖에 기준 Python 구현에 존재하는 유효성 규칙

타입이 다른 몸통과 무기는 정상 조립으로 인정하지 않습니다. UI는 명확한
오류 상태를 표시합니다.

## 3D 표시 범위

### 부품 단독 미리보기

- 다리, 몸통, 무기, 액세서리는 각각 단독 3D 미리보기를 제공합니다.
- 액세서리는 완성 유닛에 부착하지 않습니다.
- 타워링 계열도 액세서리 자체 모델만 단독으로 표시합니다.
- 별도의 타워링 유닛 모델이나 타워링 전용 조립 장면은 사용하지 않습니다.
- 서브코어는 게임처럼 아이콘으로만 표시하며 3D 모델을 사용하지 않습니다.

### 일반 유닛 조립 미리보기

조립 예시는 다리 + 몸통 + 무기만 포함합니다. 검증된 정적 변환 순서는
다음과 같습니다.

1. 다리 지오메트리에 자체 GX 변환을 적용합니다.
2. 몸통에는 자체 GX 변환 후 다리 XFI 몸통 소켓 0, 다리 주 GX 프레임
   변환 순으로 적용합니다.
3. 무기에는 자체 GX 변환 후 몸통 XFI 무기 소켓, 몸통 주 GX 프레임,
   다리 XFI 몸통 소켓 0, 다리 주 GX 프레임 순으로 적용합니다.
4. 일반 유닛의 기본 무기 소켓은 `Nova-1492-GX-Unpacker`에서 검증한
   0 기반 인덱스 2를 사용합니다.

GX 행렬과 XFI 행렬은 저장 규칙이 다릅니다. 원시 4x4 배열을 같은 규칙으로
직접 곱하지 말고 기준 도구의 GX/XFI별 변환 의미와 적용 순서를 보존합니다.

몸통과 무기의 타입이 다르더라도 진단 및 피드백을 위해 3D 표시는 시도할
수 있습니다. 이 경우 정상적인 소켓 부착은 보장하지 않으며, 반드시
`조립 불가` 상태가 3D 화면과 계산 결과에 함께 표시되어야 합니다.

### 3D 상호작용

- 부품 선택 변경 시 해당 모델을 실시간으로 갱신합니다.
- 회전, 확대/축소, 카메라 초기화를 제공합니다.
- 로딩, 파싱, 캐시 조회, 오류 상태를 사용자에게 표시합니다.
- 모델 전환 시 Three.js 지오메트리, 머티리얼, 텍스처 수명을 관리하여
  누적 메모리 사용을 방지합니다.

## 애니메이션

- 최소 지원 클립은 `idle`, `move`, `attack`입니다.
- 모델을 불러오면 `idle`을 기본으로 자동 반복 재생합니다.
- 사용자는 `idle`, `move`, `attack` 선택과 재생, 일시정지, 처음부터 재생을
  제어할 수 있어야 합니다.
- 다리, 몸통, 무기의 선택된 클립은 동일한 시점에 시작합니다.
- 세 부품의 기본 클립 길이는 동일하므로 추가적인 타임 워핑이나 재샘플링은
  요구하지 않습니다.
- 동기화의 기준은 재생 시작점을 맞추는 것이며 프레임 단위의 완전한 동기화는
  요구하지 않습니다.
- 애니메이션 병합 때문에 원본 GX/XFI 데이터나 설치 클라이언트를 변경하지
  않습니다.

## 로컬 GX/XFI 읽기

- 게임 설치 디렉터리는 외부의 읽기 전용 데이터 원본입니다.
- 웹 앱은 사용자의 명시적인 버튼 동작과 폴더/파일 선택 승인을 받은 뒤에만
  로컬 리소스를 읽습니다.
- 승인된 범위를 넘어 경로를 탐색하거나 고정 경로를 무단으로 읽지 않습니다.
- 원본 GX, XFI, 텍스처 또는 파싱 결과를 서버로 업로드하지 않는 것을
  기본 정책으로 합니다.
- 브라우저 파서는 손상되거나 예상과 다른 파일을 고려해 오프셋, 길이,
  개수, 인덱스 범위를 보수적으로 검증해야 합니다.
- 무거운 바이너리 파싱과 변환은 가능한 한 Web Worker에서 실행하여 UI
  스레드를 막지 않습니다.
- 사용자는 기본적으로 Nova 1492의 `datan/common` 디렉터리 전체를 한 번
  선택합니다. 앱은 승인된 디렉터리 안에서 파일명 인덱스를 만들고 필요한
  GX, XFI, 텍스처만 읽습니다.
- 부품명과 GX 파일의 연결은
  `../Nova-1492-GX-Unpacker/tools/export_parts_glb.py`의 검증된 매핑표를
  웹용 메타데이터로 이식하여 사용합니다.
- 부품 타입(탑형, 팔형, 어깨형)과 서브코어 ID 1~12 아이콘은 승인된
  `lab_ui.png`의 검증된 아틀라스 좌표를 사용합니다. 원본 전체를 저장소에
  포함하지 않고 브라우저 메모리에서 필요한 영역만 잘라 표시합니다.

### 브라우저별 로컬 파일 선택

- Chrome, Edge 등 지원되는 Chromium 브라우저에서는 File System Access
  API의 읽기 전용 디렉터리 핸들을 우선 사용합니다.
- Firefox와 File System Access API가 없는 브라우저에서는 디렉터리 업로드
  입력(`input[type=file][webkitdirectory][multiple]`)을 사용하여 동일한
  파일 인덱스와 파싱 파이프라인으로 연결합니다.
- Safari도 같은 폴백 경로로 동작하도록 구현하되 현재 프로젝트에서는 실제
  Safari 환경 검증을 생략합니다. Safari 지원은 미검증 지원으로 명시합니다.
- 특정 API 이름이나 User-Agent만으로 기능을 차단하지 말고 capability
  detection 후 사용할 수 있는 선택 방식을 제공합니다.

PC의 3D 기능은 사용자 승인 기반 로컬 리소스 읽기를 전제로 합니다. 모바일
환경에서는 GX 읽기와 3D 렌더링 기능을 제공하지 않습니다.

## 모델 캐시

- 쿠키에는 모델이나 덱을 저장하지 않습니다.
- 원본 GX를 복제해 저장하지 않고 변환된 GLB와 파서 메타데이터를
  IndexedDB에 저장합니다.
- 캐시 키에는 최소한 소스 식별자, 파일 크기 또는 수정 시각, 파서 버전을
  포함하여 오래된 결과를 재사용하지 않도록 합니다.
- 권한이 유지된다고 가정하지 말고 앱 재실행 시 파일 핸들의 권한을 다시
  확인합니다.
- 브라우저 저장소는 삭제되거나 용량 제한을 받을 수 있으므로 캐시 미스 시
  사용자 승인 후 다시 파싱할 수 있어야 합니다.
- 캐시는 성능 최적화이며 원본 게임 파일의 영구 백업으로 취급하지 않습니다.
- 설정 화면에 전체 모델 캐시의 크기/상태 확인과 전체 삭제 기능을
  제공합니다.

## PC 및 모바일 대응

- 계산기, 덱 관리, JSON 가져오기/내보내기는 PC와 모바일 모두 지원합니다.
- 3D 및 로컬 GX 읽기는 PC에서만 지원합니다.
- 모바일에서는 3D 캔버스와 관련 조작을 숨기거나 비활성화하고, 기능이 PC
  전용임을 명확히 안내합니다.
- 단순 User-Agent 문자열만 신뢰하지 말고 화면 크기, 입력 방식, 필요한 Web
  API 지원 여부를 함께 확인합니다.
- 터치 목표 크기, 키보드 탐색, 색상 외 오류 표시 등 기본 접근성을 지킵니다.

## 덱 요구사항

- 사용자는 여러 개의 덱을 생성할 수 있습니다.
- 각 덱에는 게임과 같이 정확히 10개의 유닛 슬롯이 있습니다.
- 슬롯은 빈 상태를 허용합니다.
- 덱 생성, 이름 변경, 복제, 삭제와 유닛 저장, 교체, 제거를 지원합니다.
- 마지막 사용 덱과 선택 슬롯을 다음 방문에서 복원합니다.
- 기본 로컬 저장소는 IndexedDB입니다. 쿠키는 사용하지 않습니다.
- 로컬 기능은 로그인이나 서버 연결 없이 동작해야 합니다.
- 초기 버전에는 계정, 원격 데이터베이스, 서버 동기화를 포함하지 않습니다.

저장되는 유닛은 최소한 다음 정보를 포함합니다.

- 유닛 이름
- 다리, 몸통, 무기, 액세서리 ID
- 서브코어 3개 ID
- 다리, 몸통, 무기의 강화 수치
- 액세서리 랜덤 옵션
- 데이터 스키마 버전과 부품 카탈로그 버전

스킬, 상태, 타워링 활성화 여부, 데스매치, 진형 등 전투 시뮬레이션 입력은
덱 유닛에 저장하지 않습니다.

## 덱 가져오기 및 내보내기

- 사람이 읽을 수 있는 버전 지정 JSON을 기본 교환 형식으로 사용합니다.
- 현재 유닛, 덱 하나, 전체 덱 백업을 내보낼 수 있어야 합니다.
- 가져오기 전에 Zod로 형식과 값 범위를 검증합니다.
- 가져오기 시 기존 데이터와 병합하거나 교체할 수 있어야 합니다.
- 구버전 스키마는 명시적인 마이그레이션을 거칩니다.
- 알 수 없는 부품 ID, 누락된 카탈로그 항목, 잘못된 수치를 사용자에게
  보고하고 가능한 범위에서 안전하게 복구합니다.
- 덱 JSON에는 GX, GLB, 텍스처 등 게임 리소스를 포함하지 않습니다.

권장 최상위 식별 필드는 다음과 같습니다.

```json
{
  "format": "nova-parts-deck",
  "schemaVersion": 1,
  "catalogVersion": "...",
  "exportedAt": "...",
  "decks": []
}
```

## 데이터 및 리소스 안전

- 설치된 Nova 1492 클라이언트의 파일을 수정, 이동, 삭제하지 않습니다.
- 게임 실행 파일, 런처, 인증, 서버 통신, DRM, 안티치트는 범위 밖입니다.
- 원본 게임 에셋을 저장소에 커밋하거나 애플리케이션 서버에서 배포하지
  않습니다.
- 테스트에는 직접 만든 최소 바이너리 픽스처, 메타데이터, 해시, 계산 결과를
  우선 사용합니다.
- 파싱한 원본 리소스를 덱 JSON이나 오류 로그에 포함하지 않습니다.

## 코드 구조 원칙

구체적인 폴더는 스캐폴딩 시 확정하되 책임을 다음처럼 분리합니다.

- `domain`: 부품 타입, 계산식, 조립 유효성, 덱 스키마
- `data`: 부품/서브코어 카탈로그와 로딩/검증
- `gx`: 로컬 권한, 파일 탐색, GX/XFI 파싱, 텍스처 처리, 캐시
- `viewer`: Three.js 장면, 부품 수명주기, 소켓 조립, 애니메이션
- `deck`: 덱 상태, IndexedDB 저장소, 가져오기/내보내기
- `ui`: 반응형 화면과 접근성

파서, 계산기, 저장소, 3D 뷰어는 인터페이스로 분리하여 UI 컴포넌트가 파일
형식이나 IndexedDB 세부사항을 직접 다루지 않도록 합니다.

## 시각 디자인 방향

- 첨부된 게임 화면의 금속 프레임, 청색 그리드, 10칸 덱과 정보 위계에서
  영감을 얻되 원본 화면을 픽셀 단위로 복제하지 않습니다.
- 현대적인 반응형 웹 UI로 재해석합니다.
- 개발 초기에 핵심 화면의 레이아웃을 먼저 만들고 데스크톱/모바일 정보
  밀도, 3D 가시성, 조작 편의성을 비교하여 적절한 레이아웃을 선택합니다.
- 시각 장식보다 계산기 입력, 유효성 상태, 덱 10슬롯, 3D 영역의 사용성을
  먼저 확정합니다.

## 검증 기준

- Python 계산기와 대표/경계/무작위 조합 결과 비교
- 강화 수치의 정수/실수 모드와 절삭 순서 테스트
- 모든 조립 불가 규칙 테스트
- GX/XFI 손상 입력과 범위 초과 입력 테스트
- 다리 → 몸통 → 무기 변환 순서 테스트
- `idle`, `move`, `attack` 동시 시작 테스트
- 캐시 적중, 무효화, 권한 소실, 저장소 삭제 복구 테스트
- 덱 10슬롯, 여러 덱, 재실행 복원 테스트
- JSON 왕복 및 구버전 마이그레이션 테스트
- 데스크톱 반응형 및 모바일 3D 비활성화 테스트

## 확정된 초기 구현 결정

1. PC 3D는 Chromium과 Firefox를 지원 대상으로 하며 Safari도 대응 코드를
   제공하지만 실제 테스트는 생략합니다.
2. 사용자는 `datan/common` 전체를 읽기 전용으로 선택합니다.
3. 부품-GX 연결은 `export_parts_glb.py`의 매핑을 기준으로 합니다.
4. GX 파서는 TypeScript Web Worker로 포팅합니다.
5. 변환 GLB와 메타데이터를 IndexedDB에 캐시하고 전체 삭제 기능을
   제공합니다.
6. 애니메이션은 `idle` 자동 반복이 기본이며 3종 클립과 기본 재생 제어를
   제공합니다.
7. 덱에는 유닛 구성과 강화 수치만 저장합니다.
8. 초기 버전은 로컬 저장만 구현합니다.
9. UI는 게임 화면을 현대적으로 재해석하고 초기 레이아웃 구현 과정에서
   적절한 배치를 선택합니다.
